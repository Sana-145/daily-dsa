#include<stdio.h>
#include<iostream>
#include<vector>
using namespace std;

vector<int> removeDuplicates(const vector<int>& arr, int n) {
    if (n == 0 || n == 1) return arr;

}

int main() {
    int n;
    cin >> n;

    vector<int> arr;

    for (int i = 0; i < n; i++) {
        cin >> arr[i];
    }
    return 0;
}